##Multiline If Then

Then should only be used in single line code not on multiline

**Example:**

```
# bad
if some_condition then
  # body omitted
end

# good
if some_condition
  # body omitted
end
```
